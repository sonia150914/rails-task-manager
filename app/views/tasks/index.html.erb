<h1>My Tasks</h1>

<% @tasks.each do |task| %>
  <li>
    <%=link_to task.title, task_path(task.id)%> <%task.details%>  <%task.completed%>
  </li>
<% end %>
<br>
<%= link_to "Create a new task", new_task_path %>
